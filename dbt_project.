name: gz_dbt_repository
version: 1.0
config-version: 2

profile: gz_dbt_repository
model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  gz_dbt_repository:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    mart:
      +materialized: table     
      +schema: finance         